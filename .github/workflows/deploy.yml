name: Deploy to AWS EC2

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: production-deploy
  cancel-in-progress: false

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
      - name: Install dependencies
        working-directory: backend
        run: pip install -r requirements.txt
      - name: Django checks
        working-directory: backend
        env:
          DJANGO_SECRET_KEY: ci-test-key
          DJANGO_DEBUG: "True"
          DATABASE_URL: sqlite:///test.db
          REDIS_URL: redis://localhost:6379/0
        run: python manage.py check

  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script_stop: true
          command_timeout: 5m
          script: |
            set -e
            cd /home/ubuntu/roombuddy
            bash scripts/deploy.sh

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-south-1

      - name: Sync website to S3
        run: |
          aws s3 sync . s3://roombuddy.co.in \
            --exclude ".git/*" \
            --exclude ".github/*" \
            --exclude "backend/*" \
            --exclude "frontend/*" \
            --exclude "*.md" \
            --exclude "*.py" \
            --exclude "*.json" \
            --exclude ".well-known/*" \
            --delete

      # Uploaded separately from the sync above so each gets the exact
      # content-type Apple/Google's link-verification fetchers require —
      # the generic sync excludes *.json and would otherwise guess wrong.
      - name: Upload app link verification files
        run: |
          aws s3 cp .well-known/apple-app-site-association \
            s3://roombuddy.co.in/.well-known/apple-app-site-association \
            --content-type "application/json"
          aws s3 cp .well-known/assetlinks.json \
            s3://roombuddy.co.in/.well-known/assetlinks.json \
            --content-type "application/json"

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }} \
            --paths "/*"
